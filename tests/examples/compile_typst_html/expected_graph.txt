root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = REPREP_TYPST_INVENTORY [amended]
                     = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   file:lorem.typ
             creates   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html

file:lorem.typ
               state = STATIC
          created by   step:./plan.py
            supplies   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html

step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html
               state = SUCCEEDED
                need = DEFAULT
           using_env = REPREP_TYPST [amended]
                     = REPREP_TYPST_ARGS [amended]
                     = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:lorem.typ
             creates   file:lorem.deps.json
             creates   file:lorem.html
            supplies   file:lorem.deps.json
            supplies   file:lorem.html

file:lorem.deps.json
               state = BUILT
          created by   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html
            consumes   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html

file:lorem.html
               state = BUILT
          created by   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html
            consumes   step:srr-compile-typst --keep-deps lorem.typ --out=lorem.html

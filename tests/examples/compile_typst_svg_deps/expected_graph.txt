root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = REPREP_TYPST_INVENTORY [amended]
                     = STEPUP_PATH_FILTER [amended]
                 ngm = ['*.typ'] {}
                 ngm = ['*.svg'] {}
                 ngm = ['*.png'] {}
          created by   root:
            consumes   file:plan.py
             creates   file:demo.typ
             creates   file:embedded.svg
             creates   file:linked.svg
             creates   file:random.png
             creates   step:srr-compile-typst --keep-deps demo.typ

file:demo.typ
               state = STATIC
          created by   step:./plan.py
            supplies   step:srr-compile-typst --keep-deps demo.typ

file:embedded.svg
               state = STATIC
          created by   step:./plan.py
            supplies   step:srr-compile-typst --keep-deps demo.typ

file:linked.svg
               state = STATIC
          created by   step:./plan.py
            supplies   step:srr-compile-typst --keep-deps demo.typ

file:random.png
               state = STATIC
          created by   step:./plan.py
            supplies   step:srr-compile-typst --keep-deps demo.typ

step:srr-compile-typst --keep-deps demo.typ
               state = SUCCEEDED
                need = DEFAULT
           using_env = REPREP_TYPST [amended]
                     = REPREP_TYPST_ARGS [amended]
                     = STEPUP_PATH_FILTER [amended]
          created by   step:./plan.py
            consumes   file:demo.typ
            consumes   file:embedded.svg [amended]
            consumes   file:linked.svg [amended]
            consumes   file:random.png [amended]
             creates   file:demo.deps.json
             creates   file:demo.pdf
            supplies   file:demo.deps.json
            supplies   file:demo.pdf

file:demo.deps.json
               state = BUILT
          created by   step:srr-compile-typst --keep-deps demo.typ
            consumes   step:srr-compile-typst --keep-deps demo.typ

file:demo.pdf
               state = BUILT
          created by   step:srr-compile-typst --keep-deps demo.typ
            consumes   step:srr-compile-typst --keep-deps demo.typ

root:
             creates   file:plan.py
             creates   step:./plan.py

file:plan.py
               state = STATIC
          created by   root:
            supplies   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [amended]
          created by   root:
            consumes   file:plan.py
             creates   st:.git/
             creates   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

st:.git/
          created by   step:./plan.py
             creates   file:.git/HEAD
             creates   file:.git/refs/heads/main

step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'
               state = SUCCEEDED
                need = DEFAULT
          created by   step:./plan.py
            consumes   file:.git/HEAD [amended]
            consumes   file:.git/refs/heads/main [amended]
             creates   file:gitlog.txt
            supplies   file:gitlog.txt

file:gitlog.txt
               state = BUILT
          created by   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'
            consumes   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

file:.git/HEAD
               state = STATIC
          created by   st:.git/
            supplies   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

file:.git/refs/heads/main
               state = STATIC
          created by   st:.git/
            supplies   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

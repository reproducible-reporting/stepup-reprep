root:
             product   file:plan.py
             product   step:./plan.py

file:plan.py
               state = CONFIRMED
             creator   root:
                sink   step:./plan.py

step:./plan.py
               state = SUCCEEDED
                need = PLAN
           using_env = STEPUP_PATH_FILTER [dynamic]
             creator   root:
              source   file:plan.py
             product   st:.git/
             product   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

st:.git/
             creator   step:./plan.py
             product   file:.git/HEAD
             product   file:.git/refs/heads/main

step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'
               state = SUCCEEDED
                need = DEFAULT
             creator   step:./plan.py
              source   file:.git/HEAD [dynamic]
              source   file:.git/refs/heads/main [dynamic]
             product   file:gitlog.txt
                sink   file:gitlog.txt

file:gitlog.txt
               state = BUILT
             creator   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'
              source   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

file:.git/HEAD
               state = CONFIRMED
             creator   st:.git/
                sink   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'

file:.git/refs/heads/main
               state = CONFIRMED
             creator   st:.git/
                sink   step:srr-wrap-git --stdout=gitlog.txt -- git log -n1 --pretty='format:%cs (%h)'
